import React from 'react';

if (typeof window != 'undefined' && window.document) require('./index.scss');

const UserBetsPopup = (props) => {
  return (
    <div className="user-bets-popup">

    </div>
  )
};

export default UserBetsPopup;